# GitHub Pages static diagnostic tool

This folder is designed for static deployment on GitHub Pages.

- `index.html` contains the browser-based MAFLD probability calculator.
- The deployed calculator uses a logistic model refit on all NHANES 2017-2023 development data.
- Full R/Shiny and top-model explanation outputs remain in `../diagnostic_web_app`, `../tables`, and `../figures`.
